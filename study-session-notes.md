# 麻雀点数記録PWA 勉強会メモ（約10分）

## タイトル
麻雀点数記録アプリをPWAで作って、GitHub Pagesで公開するまで

## 0. つかみ（30秒）
- スマホで使える麻雀点数記録アプリをPWAで作った
- オフラインで動く
- ストア申請なしで配れる

## 1. アプリ概要（1分）
- 4人麻雀の点数記録
- 1ルームに複数半荘を保存
- ウマ・オカ・場代の自動計算
- スマホ対応（入力しやすいUI調整）

## 2. 技術選定（1分）
- Flutter/React Nativeも候補
- 学習コストと公開の手軽さでPWAに決定
- React + Vite
- データ保存はIndexedDB（Dexie.js）

## 3. PWAとは（2分）
- PWA（Progressive Web App）は「Webアプリをアプリっぽく使える」仕組み
- ホーム画面に追加すると、アプリアイコンとして起動できる
- URL配布だけで導入でき、ストア申請が不要
- Service Workerが動くと、ページや画像をキャッシュできる
- その結果、オフラインでも動作できる（ただし初回はオンライン必須）
- 権限や通知などはブラウザによって制限がある
- 技術的には「HTTPS配信 + manifest + Service Worker」がセット
- manifestにはアプリ名/アイコン/起動URL/表示モードなどを書く
- Service Workerは裏側で動くスクリプトで、通信を横取りしてキャッシュを制御する
- iOSは通知やバックグラウンド制限が強め、Androidは比較的自由

## 4. データ保存の仕組み（1分）
- IndexedDBはブラウザ内のデータベース
- 文字列だけのlocalStorageと違い、オブジェクトをそのまま保存できる
- 検索や並び替えができるので、履歴一覧に向いている
- 保存先は端末内だけなので、外部に送信されない
- 別端末/別ブラウザには引き継がれない
- ブラウザのサイトデータ削除で消える
- 非同期APIなので、重い処理でも画面が止まりにくい
- テーブル（object store）とインデックスでデータを管理する
- このアプリでは「ルーム」「半荘」を別テーブルで保存している

## 5. GitHub Pagesで公開（2分）
- Viteの`base`をリポジトリ名に合わせる
- GitHub Actionsで自動デプロイ
- `public`配下はそのまま公開される
- PWAの`start_url`と`scope`を相対にするとホーム画面起動が安定

## 6. Androidストア展開（1分）
- PWAをTWAでラップするとAndroidに出せる
- アプリは「URLを開く入れ物」
- 初回はオンライン必須だが、その後はオフラインOK

## 7. ハマりどころ・学び（1分）
- `start_url`が`/`だとGitHub Pagesで404
- スマホ入力欄の幅調整が必須
- バックアップ機能は将来的な課題

## 8. まとめ（30秒）
- PWAは作りやすく、配布も簡単
- 小規模アプリに向いている
- 今後はエクスポート機能などを検討
